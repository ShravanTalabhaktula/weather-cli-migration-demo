# Weather CLI Migration Demo

A TypeScript-based command-line application that fetches live weather data by city name using Open APIs. This project demonstrates the migration of a Node.js CLI app from CommonJS to modern ECMAScript Modules (ESM) with TypeScript and Jest.

## 🌤 Features

- Fetches current weather using Open-Meteo API
- Converts city name to coordinates via Nominatim API
- Type-safe with TypeScript
- CLI interaction via readline
- Demonstrates modular Node.js structure

## 🏗 Branches

- `commonjs-ts`: CommonJS + TypeScript version
- `main`: ESM + TypeScript + Jest (final version)

## 🚀 Getting Started

### 1. Clone the repo

```bash
git clone https://github.com/YOUR_USERNAME/weather-cli-migration-demo.git
cd weather-cli-migration-demo
```
